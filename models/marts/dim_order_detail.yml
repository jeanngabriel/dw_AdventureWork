version: 2

models:
  - name: dim_order_detail
    columns:
      - name: order_id
        description: The primary key of the order

      - name: detail_sk
        description: The foreign key to the detail dimension table


      - name: product_fk
        description: The foreign key to the product dimension table